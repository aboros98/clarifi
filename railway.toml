[build]
builder = "nixpacks"
buildCommand = "pip install -e '.[dev]' && pip install -e './memU[postgres]' || true"

[deploy]
startCommand = "uvicorn clarifi.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
